# Pulses

Drop the file from Class Pulse's **Download this pulse** button here (`pulse-YYYY-MM-DD.md`). It's name-free by construction — counts, tiers, movement. On "run Monday," your AI reads the newest one, updates the dashboard, and proposes the week's moves. Keep them all; the history is your trend line.
