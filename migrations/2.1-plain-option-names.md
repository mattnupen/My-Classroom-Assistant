# Migration: 2.0 → 2.1 — plain names for the two student-information options

**Audience:** the Claude agent running an update in a teacher's project folder.
**Applies when:** `my-classroom/data-policy.md` or `my-classroom/dashboard.md` still says "Locked-Room" or "Direct" (as a mode name), or `dashboard.md` still has an "On deck" line, a "Baseline" or "Target" row, setup steps on its Later list, or no "## Later" section. Otherwise skip this file and say nothing about it.

Check-then-act; re-running changes nothing.

1. **Data policy** (only if it names a mode). Rewrite `my-classroom/data-policy.md` into the current `my-classroom.example/data-policy.md` layout, carrying over the facts: Locked-Room → **Offline**, Direct → **Claude for Teachers**; keep the date, who approved (and the basis, under Notes), and every change-log line. Add one change-log line: "[today] — option names updated; nothing else changed."
2. **Class page.** If `my-classroom/dashboard.md` names the mode (or any option) in its header line, remove it, matching `my-classroom.example/dashboard.md`; the option lives only in `data-policy.md`. Rename a "Baseline" row to "Where we started." Replace a "Target" row with a blank "This week's goal" row (the next Monday plan fills it in); the old target was for the season, not a week, so if it's worth keeping, add it to the Mission line (e.g. "… — under 50 by the finale"). If its "Current focus" section has an "On deck" line, replace the section with the template's three lines (what we're trying, the number we're watching, the next check), carrying over the current move; set the next check about two weeks out.
   If it has no "## Later" section, add the template's (from `my-classroom.example/dashboard.md`) just above "## The story so far", with any parked problems and open follow-ups as its lines. If an existing Later list holds setup steps (the practice run, naming the AI, launch, the crisis card), remove them; they aren't the teacher's chores.
3. **Class story.** Leave past entries as written — they're history.
4. **Tell the teacher,** only if their option is Claude for Teachers: "One small change: if your district's permission covers it, you can also paste student information right into our chat instead of only using the inbox folder. If you're not sure it does, keep using the inbox. Nothing else about how I handle names has changed." Offer the updated one-page record (`setup/permissions/data-mode-record.md`) in case the district wants the new wording on file. Otherwise say nothing about this migration.
